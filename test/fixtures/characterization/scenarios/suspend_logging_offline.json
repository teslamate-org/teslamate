{
  "car": {
    "efficiency": 0.153,
    "eid": 42,
    "model": "3",
    "name": "blue",
    "vid": 1000,
    "vin": "5YJ3E1EA1KF000001"
  },
  "description": "Conversion of 'immediately returns :ok if offline': the declared suspend_logging call on an offline vehicle is answered :ok without a strict fetch (pinned in the calls section) and changes nothing; the offline terminal repeats.",
  "events": [
    {
      "vehicle": {
        "state": "offline"
      }
    },
    {
      "call": "suspend_logging"
    },
    {
      "vehicle": {
        "state": "offline"
      }
    }
  ],
  "settings": {
    "suspend_after_idle_min": 100000,
    "suspend_min": 100000,
    "use_streaming_api": true
  }
}
